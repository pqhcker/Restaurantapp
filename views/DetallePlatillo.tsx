import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const DetallePedido = (): React.JSX.Element => {
  return (
    <View>
      <Text>DetallePedido</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default DetallePedido;
